# Core library dependencies (for src/ directory)
# Note: Backend-specific dependencies are in backend/requirements.txt
# Install both: pip install -r requirements.txt -r backend/requirements.txt

# Plotting (used by src/backtest/engine.py for visualization)
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# Testing
pytest>=7.0.0,<8.0.0
pytest-cov>=4.0.0,<5.0.0

# Development tools
black>=23.0.0,<24.0.0
flake8>=6.0.0,<7.0.0
mypy>=1.0.0,<2.0.0
